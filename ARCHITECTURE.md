# Архитектура проекта ShineCore Launcher

## 📁 Структура проекта

Проект организован по принципу **модульности** и **разделения ответственности**:

```
src/
├── main-new.js                 # ✨ Новая точка входа (заменяет main.js)
├── preload.js                  # Preload скрипт
│
├── core/                       # 🎯 Ядро приложения
│   ├── MainProcess.js         # Главный класс, координирует все модули
│   ├── WindowManager.js       # Управление окнами (главное, консоль)
│   ├── TrayManager.js         # Системный трей
│   ├── ConfigManager.js       # Управление конфигурацией
│   └── ProcessMonitor.js      # Мониторинг процессов Minecraft
│
├── ipc/                        # 📡 IPC обработчики (разделены по функциям)
│   ├── window-handlers.js     # Управление окнами
│   ├── config-handlers.js     # Конфигурация
│   ├── version-handlers.js    # Версии Minecraft
│   ├── modpack-handlers.js    # Модпаки
│   ├── background-handlers.js # Фон лаунчера
│   ├── console-handlers.js    # Консоль отладки
│   └── update-handlers.js     # Автообновления
│
├── loaders/                    # 🔌 Загрузчики модов (готово к расширению)
│   ├── base/
│   │   └── BaseLoader.js      # Базовый класс для всех загрузчиков
│   ├── vanilla/
│   │   ├── VanillaDownloader.js
│   │   └── VanillaIntegrity.js
│   ├── fabric/
│   │   └── FabricDownloader.js
│   └── neoforge/              # 🚀 Готово для будущего добавления
│       └── README.md          # Документация для NeoForge
│
├── game/                       # 🎮 Игровая логика
│   ├── GameLauncher.js        # Запуск Minecraft
│   ├── JavaManager.js         # Управление Java
│   └── NativeManager.js       # Управление нативными библиотеками
│
├── modpacks/                   # 📦 Модпаки
│   ├── ModpackLauncher.js     # Запуск модпаков
│   └── ModpackManager.js      # Синхронизация файлов
│
├── utils/                      # 🛠 Утилиты
│   ├── DownloadUtil.js        # Загрузка файлов
│   ├── Logger.js              # Логирование
│   └── FileUtil.js            # Работа с файлами
│
├── ui/                         # 🎨 Интерфейс
│   ├── windows/
│   │   ├── main/
│   │   │   ├── index.html
│   │   │   ├── styles.css
│   │   │   └── renderer/      # 📝 Будущее: разбитый renderer.js
│   │   └── console/
│   │       ├── console.html
│   │       └── console.js
│   └── assets/                # Ресурсы (иконки, видео)
│
└── launcher/                   # ⚠️ Старые файлы (можно удалить после тестирования)
```

---

## 🏗 Принципы архитектуры

### 1. **Модульность**
Каждый модуль отвечает за одну конкретную функцию:
- `core/` - управление приложением
- `ipc/` - коммуникация между процессами
- `loaders/` - загрузка разных типов модлоадеров
- `game/` - запуск и управление игрой
- `modpacks/` - работа с модпаками
- `utils/` - общие утилиты

### 2. **Разделение ответственности (SRP)**
- **MainProcess** - координатор, инициализирует все модули
- **WindowManager** - только управление окнами
- **TrayManager** - только системный трей
- **ConfigManager** - только конфигурация
- **ProcessMonitor** - только мониторинг процессов

### 3. **Расширяемость**
- Базовый класс `BaseLoader` для всех загрузчиков
- Легко добавить NeoForge, Forge, Quilt и другие загрузчики
- IPC handlers организованы по категориям - легко добавлять новые

### 4. **Чистый код**
- Каждый файл < 400 строк (в среднем)
- Четкая структура папок
- Понятные имена файлов и классов
- Документация в коде

---

## 🚀 Как добавить NeoForge загрузчик

1. Создайте `src/loaders/neoforge/NeoForgeDownloader.js`:
```javascript
const BaseLoader = require('../base/BaseLoader');

class NeoForgeDownloader extends BaseLoader {
    constructor(minecraftDir, logger, downloader) {
        super(minecraftDir, logger, downloader);
        this.neoForgeMetaUrl = 'https://maven.neoforged.net/releases/net/neoforged/neoforge/maven-metadata.xml';
    }
    
    getName() {
        return 'NeoForge';
    }
    
    async install(mcVersion, neoForgeVersion) {
        // Реализация установки NeoForge
    }
}

module.exports = NeoForgeDownloader;
```

2. Добавьте в `core/MainProcess.js`:
```javascript
const NeoForgeDownloader = require('../loaders/neoforge/NeoForgeDownloader');
// ...
this.neoforge = new NeoForgeDownloader(this.minecraftDir, this.logger, this.downloader);
```

3. Создайте IPC handler в `ipc/neoforge-handlers.js`

4. Готово! NeoForge интегрирован

---

## 📖 Использование новой структуры

### Запуск с новой архитектурой

1. **Обновите package.json**:
```json
{
  "main": "src/main-new.js"
}
```

2. **Или переименуйте файлы**:
```bash
# Сделайте бэкап старого main.js
mv src/main.js src/main-old.js

# Переименуйте новый main.js
mv src/main-new.js src/main.js
```

3. **Запустите**:
```bash
npm start
```

---

## 🔄 Миграция от старой структуры

### Что изменилось:

| Старый путь | Новый путь |
|------------|-----------|
| `src/launcher/logger.js` | `src/utils/Logger.js` |
| `src/launcher/download-util.js` | `src/utils/DownloadUtil.js` |
| `src/launcher/vanilla-downloader.js` | `src/loaders/vanilla/VanillaDownloader.js` |
| `src/launcher/fabric-downloader.js` | `src/loaders/fabric/FabricDownloader.js` |
| `src/launcher/game-launcher.js` | `src/game/GameLauncher.js` |
| `src/launcher/java-manager.js` | `src/game/JavaManager.js` |
| `src/launcher/modpack-launcher.js` | `src/modpacks/ModpackLauncher.js` |
| `src/launcher/modpack-manager.js` | `src/modpacks/ModpackManager.js` |

### Все импорты обновлены автоматически ✅

---

## 💡 Преимущества новой архитектуры

✅ **Легко ориентироваться** - понятная структура папок  
✅ **Легко масштабировать** - добавление новых загрузчиков за 10 минут  
✅ **Легко тестировать** - каждый модуль независим  
✅ **Легко поддерживать** - код разбит на логические части  
✅ **Готово к продакшену** - профессиональная структура  

---

## 📝 Следующие шаги

1. ✅ Протестировать новую архитектуру
2. ✅ Убедиться что все работает корректно
3. ⬜ Разбить `renderer.js` на модули (опционально)
4. ⬜ Добавить NeoForge загрузчик
5. ⬜ Добавить юнит-тесты

---

## 🤝 Вклад в проект

При добавлении нового функционала следуйте структуре:
- Загрузчики → `loaders/`
- IPC → `ipc/`
- Утилиты → `utils/`
- Игровая логика → `game/`

Сохраняйте модульность и чистоту кода! 🚀
