# 🚀 Руководство по публикации релиза ShineCore Launcher

## ✅ Шаг 1: Сборка готова!

Ваш первый релиз собран и находится здесь:
```
dist/ShineCore Launcher-Setup-2.0.0.exe
```

Размер: ~150 МБ

## 📋 Шаг 2: Создание релиза на GitHub

### Способ 1: Через веб-интерфейс GitHub (САМЫЙ ПРОСТОЙ)

1. Откройте https://github.com/Sunshainy/ShineCore-Laucher
2. Нажмите **"Releases"** справа → **"Create a new release"**
3. Заполните форму:
   - **Tag version**: `v2.0.0`
   - **Release title**: `ShineCore Launcher v2.0.0`
   - **Description**: 
     ```
     🎉 Первый релиз ShineCore Launcher!
     
     ✨ Особенности:
     - Современный UI на базе Electron
     - Автоматическое обновление через GitHub
     - Поддержка Vanilla Minecraft
     - Поддержка Fabric Mod Loader
     - Управление Java автоматически
     - Красивый прогресс-бар скачивания
     
     🔧 Требования:
     - Windows 7 и выше
     - 300 МБ свободного места
     ```

4. **Загрузите файл**: 
   - Нажмите "Attach binaries" 
   - Выберите файл `dist/ShineCore Launcher-Setup-2.0.0.exe`

5. ✅ Нажмите **"Publish release"**

### Способ 2: Через командную строку (GitHub CLI)

```powershell
# Установите GitHub CLI (если еще нет)
# https://cli.github.com/

cd "g:\my proj\ShineCore\shinecore_laucher"

# Создайте релиз с загрузкой файла
gh release create v2.0.0 `
  "dist/ShineCore Launcher-Setup-2.0.0.exe" `
  --title "ShineCore Launcher v2.0.0" `
  --notes "🎉 Первый релиз ShineCore Launcher!"
```

## 🧪 Шаг 3: Проверка работы обновлений

### Вариант 1: Тестирование локально (РЕКОМЕНДУЕТСЯ)

1. **Установите версию 2.0.0:**
   ```
   Запустите: dist/ShineCore Launcher-Setup-2.0.0.exe
   ```

2. **Создайте новую версию для теста:**
   
   Откройте [package.json](package.json) и измените версию:
   ```json
   "version": "2.0.1"
   ```

3. **Создайте новый релиз на GitHub:**
   - v2.0.1 с файлом `dist/ShineCore Launcher-Setup-2.0.1.exe`

4. **В запущенном лаунчере v2.0.0:**
   - Откройте DevTools: `F12`
   - В консоли видите логи обновления
   - Должно появиться уведомление об обновлении
   - Проверьте все кнопки: "Обновить сейчас", "Позже", "Перезапустить"

### Вариант 2: Проверка в режиме разработки

```powershell
cd "g:\my proj\ShineCore\shinecore_laucher"

# Запустите в режиме разработки с открытой консолью
npm run dev

# Должны увидеть логи:
# > App version: 2.0.0
# > Checking for update...
# > Update available: 2.0.1 (если есть более новый релиз)
```

## 📊 Что происходит автоматически:

### При запуске лаунчера:
1. ✅ Проверяет наличие обновлений на GitHub
2. ✅ Сравнивает версию (из package.json vs GitHub)
3. ✅ Если есть новая версия → показывает окно обновления
4. ✅ Позволяет выбрать: обновить сейчас или позже
5. ✅ Каждые 30 минут проверяет обновления автоматически

### В фоне:
- ✅ Скачивает обновление без вмешательства
- ✅ Показывает прогресс в процентах
- ✅ Отображает скорость загрузки
- ✅ При ошибке показывает понятное сообщение

## 🔐 Безопасность релизов:

### electron-builder автоматически:
- ✅ Компилирует код
- ✅ Добавляет цифровую подпись
- ✅ Создает NSIS инсталлятор
- ✅ Минимизирует размер файла
- ✅ Защищает от подделок

### electron-updater:
- ✅ Проверяет подпись обновлений
- ✅ Безопасно скачивает файлы
- ✅ Откатывает при ошибке установки
- ✅ Не требует администратора

## 📈 Версионирование

После первого релиза v2.0.0:

```powershell
# Для патча (bug fixes):
# 2.0.0 → 2.0.1
# npm run build:win

# Для minor обновления (новые функции):
# 2.0.0 → 2.1.0
# npm run build:win

# Для major обновления (большие изменения):
# 2.0.0 → 3.0.0
# npm run build:win
```

## 🐛 Отладка обновлений

Если обновления не работают:

1. **Проверьте логи:**
   ```
   %APPDATA%\ShineCore Launcher\logs\main.log
   ```

2. **Убедитесь что релиз опубликован:**
   ```powershell
   gh release view v2.0.0 --repo Sunshainy/ShineCore-Laucher
   ```

3. **Проверьте .exe в релизе:**
   - Файл должен быть с расширением `.exe`
   - Имя должно быть точно как в конфиге

4. **Посмотрите что ищет автообновлятель:**
   - Откройте `src/main.js`
   - Строка ~170: `autoUpdater.checkForUpdates()`

## ✨ Готово к релизу!

Теперь можете:
- ✅ Публиковать новые версии на GitHub
- ✅ Пользователи будут получать автоматические уведомления
- ✅ Обновления устанавливаются одним кликом
- ✅ Красивый UI во время обновления

Успеха! 🚀
