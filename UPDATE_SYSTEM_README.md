# 🚀 Система автообновлений ShineCore Launcher

## ✅ Что уже сделано

### 1. **Модуль UI обновлений** (`src/ui/update-notification.js`)
- ✅ Красивое модальное окно с анимациями
- ✅ Прогресс-бар скачивания с процентами
- ✅ Отображение скорости загрузки и размера файлов
- ✅ Обработка всех событий: `update-available`, `download-progress`, `update-downloaded`, `update-error`
- ✅ Автоматическая проверка обновлений через 3 секунды после запуска

### 2. **HTML + CSS** (добавлено в `src/ui/index.html`)
- ✅ Модальное окно с полупрозрачным оверлеем и blur эффектом
- ✅ Современный дизайн в стиле вашего лаунчера (тёмная тема, градиенты)
- ✅ Анимированный прогресс-бар с shimmer эффектом
- ✅ Адаптивные кнопки с hover эффектами

### 3. **API обновлений** (обновлён `src/preload.js`)
- ✅ `checkForUpdates()` - проверка обновлений
- ✅ `startUpdateDownload()` - запуск скачивания
- ✅ `quitAndInstall()` - перезапуск для установки
- ✅ События: `onUpdateAvailable`, `onUpdateDownloadProgress`, `onUpdateDownloaded`, `onUpdateError`

### 4. **Main процесс** (обновлён `src/main.js`)
- ✅ Отправка событий в рендерер вместо системных диалогов
- ✅ Автоматическая проверка обновлений при запуске
- ✅ Проверка каждые 30 минут
- ✅ IPC обработчики для связи с UI

### 5. **Зависимости** (обновлён `package.json`)
- ✅ Добавлен `electron-log` для логирования

## 🎨 Как выглядит UI

### Доступно обновление
```
🎉 Доступна новая версия!
Версия 2.1.0 готова к установке
Улучшения производительности и исправления ошибок

[⬇️ Обновить сейчас]  [Позже]
```

### Скачивание
```
⬇️ Скачивание обновления...
Пожалуйста, подождите...

[████████████░░░░░░░░] 60%
45.2 МБ / 75.8 МБ     2.3 МБ/с
```

### Готово к установке
```
✅ Обновление готово!
Версия 2.1.0 успешно загружена
Перезапустите лаунчер для установки

[🔄 Перезапустить сейчас]  [Позже]
```

### Ошибка
```
❌ Ошибка обновления
Не удалось загрузить обновление
[детали ошибки]

[Закрыть]
```

## 🧪 Тестирование

### 1. Тест в режиме разработки
```bash
npm run dev
```

### 2. Тестирование с реальными обновлениями

#### Шаг 1: Создайте первый релиз
```bash
# Убедитесь, что версия в package.json = 2.0.0
npm run build:win

# Перейдите на GitHub → Releases → Draft a new release
# Tag: v2.0.0
# Загрузите файл: dist/ShineCore-Launcher-Setup-2.0.0.exe
# Опубликуйте релиз
```

#### Шаг 2: Установите версию 2.0.0
- Скачайте и установите `.exe` из релиза v2.0.0
- Запустите лаунчер
- Через 3 секунды должна произойти проверка обновлений
- Пока обновлений нет - ничего не покажется

#### Шаг 3: Создайте новый релиз v2.0.1
```bash
# Измените версию в package.json на 2.0.1
npm run build:win

# Создайте новый релиз v2.0.1 на GitHub
# Загрузите новый .exe файл
# Опубликуйте релиз
```

#### Шаг 4: Проверьте автообновление
- Запустите установленную версию 2.0.0
- Через 3 секунды появится модальное окно: "🎉 Доступна новая версия 2.0.1!"
- Нажмите "Обновить сейчас"
- Наблюдайте прогресс-бар скачивания
- После скачивания появится "✅ Обновление готово!"
- Нажмите "Перезапустить сейчас"
- Лаунчер перезапустится с версией 2.0.1

## 📝 Важные моменты

### Автоматические проверки
- При запуске лаунчера (через 3 секунды)
- Каждые 30 минут во время работы

### Логирование
Все события автообновления логируются в:
- **Windows**: `%USERPROFILE%\AppData\Roaming\shinecore-launcher\logs\`
- Консоль разработчика (F12 в dev режиме)

### Поведение
- `autoDownload = false` - пользователь всегда видит UI и решает сам
- `autoInstallOnAppQuit = true` - установка при выходе (если не нажал "Перезапустить")
- Обновления скачиваются в фоне без блокировки лаунчера

## 🎯 Что дальше?

### Для первого релиза:
1. ✅ Убедитесь что версия в `package.json` правильная
2. ✅ Соберите билд: `npm run build:win`
3. ✅ Создайте релиз на GitHub с тегом `v2.0.0`
4. ✅ Загрузите `.exe` файл в релиз
5. ✅ Опубликуйте релиз
6. ✅ Пользователи установят эту версию

### Для следующих обновлений:
1. Увеличьте версию в `package.json` (например, `2.0.1`, `2.1.0`)
2. Внесите изменения в код
3. Соберите новый билд: `npm run build:win`
4. Создайте новый релиз на GitHub
5. Загрузите новый `.exe`
6. Опубликуйте - и ВСЕ пользователи получат уведомление!

## 🔧 Настройки (опционально)

### Изменить интервал проверки
В `src/main.js`:
```javascript
setInterval(() => {
  autoUpdater.checkForUpdates();
}, 10 * 60 * 1000); // 10 минут вместо 30
```

### Отключить автоматическую проверку при запуске
В `src/ui/update-notification.js` закомментируйте:
```javascript
// setTimeout(() => {
//   this.checkForUpdates();
// }, 3000);
```

### Кастомизировать тексты
Все тексты находятся в `src/ui/update-notification.js` в методах:
- `showUpdateAvailable()` - окно "доступна новая версия"
- `showDownloadProgress()` - прогресс скачивания
- `showUpdateReady()` - готово к установке
- `showUpdateError()` - ошибка

## 🐛 Отладка

### Проблема: Не появляются обновления
1. Проверьте логи: `%APPDATA%\shinecore-launcher\logs\`
2. Откройте DevTools (F12) и смотрите Console
3. Убедитесь что на GitHub есть релиз с версией ВЫШЕ текущей
4. Проверьте правильность `owner` и `repo` в `package.json`

### Проблема: Ошибка скачивания
- Проверьте интернет соединение
- Убедитесь что `.exe` файл загружен в релиз на GitHub
- Проверьте что релиз опубликован (не draft)

### Проблема: Модальное окно не появляется
1. Откройте DevTools и проверьте ошибки в Console
2. Убедитесь что `update-notification.js` загружен
3. Проверьте что `#updateModal` существует в DOM

## ✨ Готово!

Теперь у вас полностью рабочая система автообновлений в стиле современных лаунчеров!
Пользователи будут получать красивые уведомления о новых версиях прямо в интерфейсе лаунчера 🎉
